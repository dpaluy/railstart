interface:
  display_name: "Railstart"
  short_description: "Use railstart to generate Rails apps and presets"
  default_prompt: "Use $railstart-coder to initialize Railstart config, generate a Rails app, author a preset, troubleshoot a run, or modify the gem."
